# Point this at YOUR deployed gateway backend (which holds the secret API key).
# e.g. https://your-gateway.onrender.com/v1
VITE_GATEWAY_URL=http://localhost:8080/v1

# Optional: must match STOREFRONT_TOKEN in your gateway (if you set one).
VITE_STOREFRONT_TOKEN=

VITE_OPERATOR_NAME=CashPost
